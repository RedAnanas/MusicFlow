# 首页视觉验收

- 视觉真源：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-b013beac-834f-41ca-8934-b41d7810e53d.png`
- 实现页面：`http://127.0.0.1:3000/`
- 桌面验证视口：2048 × 950 CSS px，设备像素比 1。
- 状态：已加载本机真实曲库和任务数据。

## 比对结果

### 字体与层级

欢迎区使用大号深色标题、浅紫色眉题和较小的说明文字；六张统计卡的标签、数字层级与视觉真源一致。

### 间距与布局

顶栏采用外层 22px 圆角、内嵌导航胶囊容器；首页主卡采用 30px 圆角，左侧文字和右侧深色视觉卡按截图比例排列。统计卡为六列等宽布局，在窄屏下降为两列。

### 色彩与组件

激活导航项使用 #aa86fb 背景与白色文字，不再使用负层级伪元素。顶栏、统计卡和操作按钮均统一了较大的圆角与轻阴影。

### 图片与内容

右侧使用原创霓虹音乐工作室插画 `frontend/src/assets/images/dashboard-neon-studio.png`，不复用截图中的图片、人物、Logo 或文字。所有统计数据仍来自 MusicFlow store。

### 交互验证

- 已在浏览器确认首页激活导航的计算样式为 `rgb(170, 134, 251)` 背景、白色文字。
- 已验证“操作台”切换至 `/files` 后，激活导航仍保持同一背景与文字颜色。

## 已知有意差异

- 截图中的“正在播放”与播放次数属于音乐播放器业务；MusicFlow 使用“开始处理”和真实转换任务统计替代。
- 截图有六个主菜单，MusicFlow 保留其七条既有功能路由。

final result: passed

---

# Apple Music 曲库候选布局修复（2026-09-22）

- 视觉真源：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-e1638060-2835-41b2-bd57-d255daca46cc.png`
- 实现截图：Codex 内置浏览器中的并排捕获；可复查页面为 `temp/design-qa/apple-catalog-comparison.html`。
- 真源尺寸：2560 × 1187 px；实现 iframe：2560 × 1187 CSS px；两者在对比页中使用相同缩放比例。
- 状态：桌面深色主题，搜索“半城烟沙”，Apple Music 候选与曲目结果均已加载。

## 比对结果

- 字体与层级：曲名、歌手与专辑继续使用现有 Musicdl 字体层级；操作文字保持单行，不再被拆成竖排。
- 间距与布局：封面和曲目信息位于首行，两个操作按钮使用整张卡片宽度的第二行；候选区只保留横向滚动，不再产生内部纵向滚动条。
- 色彩与组件：延续现有深紫背景、低对比边框与浅紫操作按钮，未改变相邻搜索结果区。
- 图片质量：继续使用 Apple 曲库返回的封面，不新增或替换图片资源；封面比例和裁切保持一致。
- 文案与内容：“在 Apple Music 中打开”和“已添加”完整可读，超长曲名、歌手和专辑仍单行省略。

## 对比历史

- P1：操作链接被放入 44px 封面列，文字强制换行并撑高卡片，继而触发候选区纵向滚动和裁切。
- 修复：增加整卡宽度的操作行，明确卡片两行网格，并将列表纵向溢出设为隐藏。
- 修复后证据：并排全页和候选区聚焦对比中，卡片高度一致，按钮完整显示，纵向滚动条消失，曲目表格恢复紧邻候选区。

final result: passed

---

# 发现音乐与 Musicdl 播放器复刻验收（2026-09-22）

- 视觉真源：用户提供的 Musicdl 搜索、底部播放器及歌词抽屉截图。
- 实现页面：`http://127.0.0.1:3000/discovery?q=稻香`。
- 桌面验证视口：2048 × 950 CSS px；使用 musicdl 实时搜索结果验证。

## 布局与视觉

- 搜索区、音乐源、格式与来源筛选、结果表头和曲目行保持 Musicdl 的深色播放器层级。
- 结果操作区固定容纳试听、下载及 MusicFlow 双库补齐状态，页面 `scrollWidth` 为 2033px，小于 2048px 视口，无横向溢出。
- 底部播放器为单行三段布局：左侧封面和曲目信息，中间上一首、播放/暂停、下一首及进度条，右侧实时频谱和音量；不再出现音量掉到下一行的问题。
- 390px 右侧歌词抽屉与参考一致，当前歌词使用高对比粗体，其余歌词使用低对比文字。

## 功能验证

- 搜索使用 SSE 逐条展示结果，实际检索“稻香”成功返回多来源曲目。
- 来源筛选选择 QQ 后只保留 1 条 QQ 结果，筛选值与 musicdl 返回的短来源标识一致。
- 实际播放酷我 FLAC 成功；播放器显示总时长、播放进度，并绘制实时频谱。
- 音频代理可返回 HTTP 206，测试区间 `bytes=1000000-1001023` 返回 1024 字节及正确 `Content-Range`。
- 点击歌词“童年的纸飞机”后播放位置跳至约 119 秒，歌词抽屉自动滚动至该行并正确高亮。
- 下载入口使用代理流并显示下载进度抽屉；播放器支持空格播放/暂停、Alt+左右方向键切歌。

## 有意保留的差异

- 保留 MusicFlow 顶部主导航和双库补齐入口，使发现音乐仍属于 MusicFlow 的完整工作流。
- 搜索结果行增加双库检查/补齐状态，不改变 Musicdl 原有试听与下载操作。

final result: passed

## 监控目录编辑页对齐复核（2026-09-17）

### 验证

- 监控目录新建与编辑弹窗统一为与转换方案编辑页相同的 880px 宽度、连续分区卡、紫色编号和固定底部操作栏。
- 三个业务分区分别保留下载入口、输出规则和自动处理的完整功能，并使用与方案编辑页相同的标题、说明、双列控件、提示条和卡片边距。
- 已在真实监控目录上打开“编辑”，确认对话框正常展示、规则可见且没有控制台错误。

final result: passed

## 转换方案与监控目录对齐复核（2026-09-17）

### 目标

将监控目录从旧的宽面板和大尺寸双列按钮，改为与转换方案详情相同的弹窗骨架；列表也采用同样的表头、行和行内操作模式。

### 验证

- 在 `http://127.0.0.1:3002/watch-folders` 打开真实监控目录详情，确认弹窗宽度为 1180px，水平与垂直均居中。
- 确认详情使用与转换方案一致的标题区、紧凑顶部操作区、双列信息卡和通栏规则卡；不再存在底部大尺寸双列按钮。
- 确认监控目录列表含详情、编辑、删除、扫描和启停操作，删除入口与方案列表保持同级可见。
- 确认转换方案列表首行仅在鼠标悬停时展示高亮，不再保留默认选中状态。

final result: passed

---

# 监控目录刷新与列表验收

- 详情打开改为仅由用户点击“详情”触发；刷新不会再绑定表格 current-change。
- 已在真实监控目录打开详情后等待一个 5 秒刷新周期，弹窗持续保持打开，未出现自动关闭或重新开启。
- 监控目录列表改为与转换方案一致的表头、圆角行和行尾操作；转换方案首行不再使用常驻选中态。
- 两类详情弹窗均强制限制为 1180px 最大宽度，窄屏仍保持视口内自适应。

final result: passed

---

# 监控目录统一交互验收

- 监控目录详情弹窗提升至 1180px 宽，并以浅紫卡片分区呈现运行信息、输出规则和事件。
- 监控目录创建与编辑已替换为单页分区表单：下载入口、输出规则、自动处理连续展示。
- 已在本机真实目录数据上打开编辑页，确认底部操作固定、表单可滚动、输出规则可见且不需要切换折叠面板。

final result: passed

---

# 编辑交互与导航验收

- 移动端底部导航已加入转换方案，六个入口等宽展示。
- 转换方案详情桌面弹窗宽度提升至 1180px；桌面详情区不再限制内部高度，避免出现内容区滚动条。
- 点击详情内编辑会先关闭详情，再显示编辑界面，不会叠加两层弹窗。
- 转换方案创建与编辑移除分步 Tab，改为同一页的五个连续配置分区；监控目录编辑默认展开全部配置分区。

final result: passed

---

# 列表与弹窗统一验收

- 转换方案、监控目录、系统设置顶部均采用操作中心同款圆角浅紫概览区。
- 转换方案已验证为列表行，行尾提供详情、编辑、删除；监控目录表格行尾提供详情、编辑、扫描、启停。
- 方案详情在手机窄屏实测为可视高度内滚动弹窗，内容不会被页面底部截断；编辑弹窗与目录编辑弹窗使用相同的最大宽度和滚动规则。
- 运行日志移动端按时间与等级、模块、摘要三行展示，避免模块名称与摘要同一行导致覆盖。

final result: passed

---

# 操作中心与设置页二次验收

- 参考：用户提供的操作记录移动端与运行日志桌面截图。
- 操作记录使用 100 条/页、运行日志使用 200 条/页，均复用操作台分页组件。
- 日志网格为时间、等级、模块和摘要四列；模块与摘要均可收缩，窄屏会换为两列卡片信息，避免重叠。
- 转换方案与监控目录已验证为“列表点击 + 居中详情弹窗”，不再存在右侧详情栏；详情弹窗限制最大宽度，适配手机窄屏。
- 系统设置统一为紫色边框、卡片和移动端单列布局。

final result: passed

---

# 操作中心视觉验收

- 视觉真源：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-f62903f7-0103-42df-aa0e-4920864df17e.png`、`C:\Users\32392\AppData\Local\Temp\codex-clipboard-d8e121f8-29dd-4e44-8c4e-9e612eec3f03.png`
- 实现页面：`http://127.0.0.1:3002/tasks`，已使用本机真实 279 条任务和运行日志验证。

## 验收结果

- 桌面导航只保留“操作记录”；原 `/logs` 地址会跳转到同页“运行日志”页签。
- 操作记录行仅显示勾选、文件名、转换方案、状态、转换进度和时间；不再显示封面、完整路径或任务详情面板。
- 运行日志改为时间、等级、模块和单行摘要列表；不再显示检测详情面板。
- 窄屏下页签横向排列，任务记录与日志按卡片式两列信息自动换行，底部移动导航不会遮挡内容。

final result: passed

---

# 移动端清单与辅助弹窗复验

- 参考：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-3c87c651-75cd-47e7-8874-8aeb335f4793.png`
- 首页：恢复六张统计卡，未恢复插画主视觉。
- 清单：移动端仅显示文件信息和操作两列；查看、转换、删除保持同一行；分页独立于文件操作栏。
- 弹窗：文件详情、选择服务器文件和选择服务器文件夹使用统一紫色圆角视觉，并在小屏使用可视区域高度与固定底部操作。
- 验证：480×1000 实测文件详情和服务器文件选择弹窗可用；首页统计卡当前页面正常渲染。

final result: passed

---

# 操作台与转换弹窗移动端适配

- 参考：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-7dfca7c2-da3a-4507-a707-e5647fc0c81d.png`、`C:\Users\32392\AppData\Local\Temp\codex-clipboard-d4d06259-78c0-4c05-8f14-4668c1c8ff2b.png`、`C:\Users\32392\AppData\Local\Temp\codex-clipboard-b59df95e-e1de-4276-b0d3-d5daadf4341c.png`
- 桌面验收：1440×900 下转换弹窗限制在可视区域，内容区独立滚动，标题和页脚操作始终可见。
- 移动验收：小屏下弹窗保留固定操作栏；操作台收窄文件列、压缩文件夹区和工具栏；主导航变为底部五项入口。
- 首页验收：已移除“音乐总览”主视觉和统计卡，仅保留转换任务概览及运行状态。
- 页面控制台：无错误。

final result: passed

---

# 操作台清单与输出规则二次验收

- 视觉真源：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-5df2b627-bdc2-42b7-92ed-4090e5e3b875.png`、`C:\Users\32392\AppData\Local\Temp\codex-clipboard-52851cc6-3f5c-4917-bba2-c35b4df65df7.png`、`C:\Users\32392\AppData\Local\Temp\codex-clipboard-be655a03-cb08-48d4-b256-41e2b409707f.png`
- 实现截图：Codex 内置浏览器 `http://127.0.0.1:3001/files` 的同状态渲染截图（本次浏览器会话捕获）。
- 状态：真实曲库已加载；默认音乐清单和单文件输出规则窗口。

## 比对结果

### 字体、布局与色彩

文件页默认直接显示音乐清单，左侧文件夹、右侧表格和底部浮动操作栏保持截图的全宽工作台布局。转换窗口使用工作台的浅灰紫边框、紫色步骤状态、圆角规则卡和低对比阴影；在窄视口改为单列表单，不出现水平溢出。

### 图片与交互

已移除空状态耳机插画。曲目封面、曲名与信息不再是可点击控件；仅保留“查看”“转换”“删除”三个明确操作。转换窗口中可添加多条输出规则，并可逐条切换“转换输出”或“原样复制”。

### 规则行为

文件页使用与监控目录相同的 `convert` / `copy` 输出目标模型。转换任务继续走既有队列；原样复制使用临时文件后原子替换，遇到同名文件安全跳过，均不覆盖现有内容。

### 浏览器验证

- 默认进入 `/files` 后直接展示真实的 2 条音乐清单。
- 选择“转换”后显示新的规则窗口；切换为“原样复制”会隐藏转换方案并保留输出目录。
- 小窗口下规则表单改为单列，未见横向滚动条。

final result: passed

---

# 转换窗口侧栏边界修正

- 参考：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-ce5f7f3b-43a8-487a-a02a-05e97a9a5b4a.png`
- 修正：移除已选文件侧栏的负外边距，避免它侵入弹窗标题区和底部操作区。
- 验证：本地预览打开单文件转换窗口，窄屏响应式布局正常；桌面布局使用同一内容区边界，不再存在负向偏移。

final result: passed

---

# 操作台视觉验收

- 视觉真源：`C:\Users\32392\AppData\Local\Temp\codex-clipboard-74ea0f6d-bc6d-499c-9668-7d841a48e47a.png`
- 实现截图：Codex 内置浏览器中 `http://127.0.0.1:3001/files` 的同状态渲染截图（本次浏览器会话捕获）。
- 比对视口：2560 × 1187 CSS px，设备像素比 1；视觉真源为 2560 × 1187 px。
- 状态：本机真实曲库已加载；默认未选中歌曲的空状态。

## 比对结果

### 字体与层级

保留现有全局字体栈，采用深色粗体的两行空状态标题、浅紫色眉题和较弱的说明文字；顶部工具栏、文件夹栏与浮动操作栏均使用较小、清晰的操作文案。

### 间距与布局

操作台从应用内容区全宽铺开，顶部工具栏高度为 66px，左侧目录为 470px，右侧为空白详情工作区。详情卡使用大圆角、浅边框、柔和阴影，并在工作区中置；底部操作条固定于面板内底部居中。

### 色彩与组件

目录选中态与“更多操作”均为参考图对应的淡紫/紫色体系；页面背景、分隔线、输入框和圆角统一为低对比浅灰紫，不再使用原操作台的绿色卡片样式。

### 图片与内容

右侧使用原创耳机、唱片与播放器插画 `frontend/src/assets/images/workbench-empty-headphones.png`，未复用截图中的人物、耳机或唱片图。实际目录、曲目和文件信息仍来自 MusicFlow store。

### 交互验证

- 已在浏览器验证顶部筛选、搜索和刷新控件可见。
- 已验证“音乐清单”会展示本机 2 条真实曲目；点击一条曲目后，右侧切换为该文件的封面、参数、完整度与转换入口。
- 已验证默认加载不自动选中曲目，保持参考图的空状态。
- 已检查浏览器控制台，无 error 级错误。

## 已知有意差异

- 原截图中的图标组合属于 MusicTag 的专有工作流；MusicFlow 用现有的添加文件、添加文件夹、清单、刷新、转换、详情、删除和清除选择入口承载等价操作。
- 插画为原创资产，避免复制参考图中的素材。

final result: passed

---

# 发现音乐下载工作流验收（2026-09-22）

- 视觉真源：用户提供的 Musicdl 搜索结果、下载按钮、右下角下载入口及原项目下载设置/下载列表界面。
- 实现页面：`http://127.0.0.1:3000/discovery?q=你若成风`。
- 验证视口：2048 × 950 CSS px，桌面深色主题。
- 实现证据：Codex 内置浏览器中捕获的下载设置抽屉、目录选择对话框及下载完成列表。

## 比对结果

- 字体与层级：设置标题、目录名称、路径、下载曲名、状态与速度沿用 Musicdl 的主次文字层级；当前 MusicFlow 字体栈与页面其余区域一致。
- 间距与布局：下载设置位于右侧抽屉，目录选择为居中对话框，下载列表为右侧抽屉，右下角常驻下载入口；页面无水平溢出。
- 色彩与组件：保持 Musicdl 深紫黑背景、粉橙主按钮、低对比边框和蓝色“保存到本机”链接；未新增近似图标资产，继续使用 Element Plus 图标。
- 图片质量：下载工作流不包含新增位图；曲目封面继续使用 musicdl 返回资源，Musicdl 标志使用原项目 SVG。
- 文案与内容：下载设置、目录配置、添加目录、选择下载目录、准备中、实时大小/速度、完成和保存到本机均与原工作流对应。

## 交互验证

- 成功读取 musicdl 已保存目录“win本地 / D:\\Music\\下载”。
- 点击歌曲“下载”先显示目录选择，不直接触发浏览器下载。
- 选择目录后由 musicdl 创建服务器下载任务，下载列表实时接收 SSE 进度。
- 实测 56.1 MB 曲目完成后进度达到 100%，显示实际大小、完成状态和“保存到本机”链接。
- 发现并修复了下载完成但列表停留在“准备中”的响应式对象问题，复验通过。

## 对比历史

- P1：首轮实测服务器已完成下载，但下载列表未刷新。修复为更新 Vue 数组中的响应式任务对象。
- 修复后证据：同一流程显示 `56.1 MB / 56.1 MB`、`完成`，进度条为 100%，保存链接可见。

final result: passed
