# 服务器配置
HOST=127.0.0.1
PORT=8082
DEBUG=false

# 目录配置，请按本机环境修改
MUSIC_SOURCE_DIR=D:/Music/source
MUSIC_OUTPUT_DIR=D:/Music/output
MUSIC_ARCHIVE_DIR=D:/Music/archive

# 留空时使用项目根目录下的 config、logs、temp
# CONFIG_DIR=D:/path/to/MusicFlow/config
# LOGS_DIR=D:/path/to/MusicFlow/logs
# TEMP_DIR=D:/path/to/MusicFlow/temp

# FFmpeg 工具路径
FFMPEG_PATH=D:/path/to/ffmpeg.exe
FFPROBE_PATH=D:/path/to/ffprobe.exe

# 任务配置
MAX_CONCURRENT_TASKS=2
FFMPEG_THREADS=2
FILE_STABLE_SECONDS=30
